@page
@model AspNetCoreIdentity.Pages.ShowRolesModel
@{
}
<a asp-page="/Roles">Show Roles</a>
<table class="table table-bordered">
    <tr>
        <th>Id</th>
        <th>RoleName</th>
    </tr>
    @foreach (var item in Model.ListRoleModel)
    { 
<tr>
    <td>@item.Id</td>
    <td>@item.Name</td>
</tr>
    }
</table>